<%= content_tag :div, class: 'media', id: "item-#{item.id}" do %>
  <%= item.name %> <%= link_to "", user_item_path(@user, item), method: :delete, class: 'glyphicon glyphicon-ok', remote: true %><br/><br/>
<% end %>