<% if @item.destroyed? %>
  $('#item-' +<%= @item.id %>).fadeOut(500);
  // $('.alerta').fadeOut(500);
  <% else %>
  $('#item-' +<%= @item.id %>).prepend("<div class='alert alert-danger'><%= flash[:error] = 'There was an error deleting this item' %></div>");
  $('.alert').fadeOut(1000);
<% end %>